# Web UI dependencies for AutoNet
# Install with: pip install -r requirements-webui.txt

# Include base production requirements
-r requirements.txt

# Django web framework
django>=4.2.0,<5.0.0                      # Django web framework (LTS version)
django-environ>=0.11.0,<1.0.0             # Environment variable management

# WSGI server for production
gunicorn>=21.2.0,<22.0.0                  # Production WSGI server
whitenoise>=6.5.0,<7.0.0                  # Static file serving

# Database support (optional, SQLite is built-in)
# Uncomment if using PostgreSQL:
# psycopg2-binary>=2.9.0,<3.0.0           # PostgreSQL adapter

# Uncomment if using MySQL:
# mysqlclient>=2.2.0,<3.0.0               # MySQL adapter

# Caching and performance (optional)
# Uncomment if using Redis:
# redis>=5.0.0,<6.0.0                     # Redis client
# django-redis>=5.3.0,<6.0.0              # Django Redis cache backend

# API support (optional)
# djangorestframework>=3.14.0,<4.0.0      # REST API framework
# django-cors-headers>=4.2.0,<5.0.0       # CORS support

# Async task processing (optional)
# Uncomment if using Celery:
# celery>=5.3.0,<6.0.0                    # Distributed task queue
# django-celery-beat>=2.5.0,<3.0.0        # Periodic task scheduler
# django-celery-results>=2.5.0,<3.0.0     # Task result backend

# Security enhancements
django-csp>=3.7,<4.0                      # Content Security Policy
django-permissions-policy>=4.18.0,<5.0.0  # Permissions-Policy header

# Monitoring and debugging (development only)
# django-debug-toolbar>=4.2.0,<5.0.0      # Debug toolbar (dev only)
# django-extensions>=3.2.0,<4.0.0         # Useful extensions (dev only)
